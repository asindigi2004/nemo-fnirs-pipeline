# NEMO fNIRS pipeline — pinned dependencies
# Install with: pip install -r requirements.txt
# Python 3.9+ required. Tested on 3.10 and 3.11.
#
# scipy >= 1.11 is required for scipy.stats.false_discovery_control.
# The pipeline includes a manual BH fallback for scipy < 1.11,
# but pinning >= 1.11 ensures the native implementation is used.

mne>=1.4.0
scipy>=1.11.0
numpy>=1.24.0
pandas>=1.5.0
matplotlib>=3.6.0
openpyxl>=3.0.10
